import { dashboardContent } from "@/constants";
import { FeaturePlaceholder } from "@/components/shared";

export function DashboardSection() {
  return <FeaturePlaceholder {...dashboardContent.dashboard} />;
}
