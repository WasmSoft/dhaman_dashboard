// AR: صفحة العملاء — ملف التوجيه فقط يجلب القسم الرئيسي.
// EN: Clients page — route file only composes the main section.
import { ClientsSection } from "@/components/clients";

export default function ClientsPage() {
  return <ClientsSection />;
}