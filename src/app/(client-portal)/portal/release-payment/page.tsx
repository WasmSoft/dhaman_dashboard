// AR: صفحة إصدار الدفعة القديمة — تم استبدالها.
// EN: Legacy mock release payment page — replaced by token-scoped live pages.
import { notFound } from "next/navigation";

export default function LegacyReleasePaymentPage() {
  notFound();
}
