// AR: صفحة سجل الدفعات القديمة — تم استبدالها بالصفحات المباشرة.
// EN: Legacy mock payment history page — replaced by token-scoped live pages.
import { notFound } from "next/navigation";

export default function LegacyPaymentHistoryPage() {
  notFound();
}
