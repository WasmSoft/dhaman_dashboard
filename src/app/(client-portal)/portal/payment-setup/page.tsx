// AR: صفحة الدفع التجريبي القديمة — تم استبدالها بالصفحات المباشرة برمز الوصول.
// EN: Legacy mock payment setup page — replaced by token-scoped live pages.
import { notFound } from "next/navigation";

export default function LegacyPaymentSetupPage() {
  notFound();
}
