// AR: صفحة مراجعات الذكاء الاصطناعي — ملف التوجيه فقط يجلب القسم الرئيسي.
// EN: AI Review page — route file only composes the main section.
import { AIReviewSection } from "@/components/ai-review";

export default function AIReviewPage() {
  return <AIReviewSection />;
}